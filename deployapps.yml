---
# Deploy one ore more apps on Windows Hosts
- hosts: windows
  # become: true
  # become_method: runas

  tasks:
    - name: Deploy applications on "{{ ansible_hostname }}"
      win_chocolatey:
        name: "{{ item }}"
        state: present
      with_items: "{{ apps }}"
